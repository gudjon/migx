# Architecture — EXO spike (draft)

```text
fixtures/songs/*.ontology.json  →  session.json graph
                ↓
        agent reasons transition (files only)
                ↓
        TRANSITION-PROOF.md  (+ P-08 evaluator)
```

- No ControlObject writes in this dossier.  
- Live co-pilot is a later EXO wave.
